'''
Train the agent on variables such as:
- number of levels
- different hyperparameters

Save run
'''

def train(levels, params):
    #Initialize new agent and train on specified number of levels
    return


if __name__ == "__main__":
    args =
    levels = args[1]
    params = args[2]
    train(levels, params)